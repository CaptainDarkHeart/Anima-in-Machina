# AI DJ MCP Server — Core Dependencies (v0.2.0, Traktor-first)

# MCP Protocol
mcp>=1.0.0

# Audio Processing (librosa = secondary/optional; soundfile for audio loading)
librosa>=0.10.0
soundfile>=0.12.1

# Scientific Computing
numpy>=1.26.0
scipy>=1.11.0

# NML parsing uses stdlib xml.etree.ElementTree — no extra XML dep needed

# Development (optional)
pytest>=7.0.0
black>=23.0.0
ruff>=0.1.0
